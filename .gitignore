/types
/results
/blanket
/blanket-*
blanket.db
# The lock-holder sidecar and the backups a local run leaves beside it
# (turtlemonvh/blanket#23 phase 4).
blanket.db.lock.json
/backups
blanket.json
blanket.toml
blanket.yml
blanket.yaml
worker.*.log
tests.xml
# Release artifacts: `make checksums` writes SHA256SUMS beside the
# cross-compiled binaries, and `make bundle` writes dist/ (#23 phase 6).
/SHA256SUMS
/dist
tests/e2e/node_modules
tests/e2e/test-results
tests/e2e/blanket.db
tests/e2e/playwright-report

# Per-developer Claude Code state — settings, scheduled-task locks, etc.
# stays ignored; committed skills under .claude/skills/ and the night-crew
# loop config are the exceptions.
.claude/*
!.claude/skills/
!.claude/night-crew.json
