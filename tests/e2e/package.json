{
  "name": "blanket-e2e",
  "version": "1.0.0",
  "description": "Playwright browser tests for blanket",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0"
  }
}
